# План выполнения task3 — строго по требованиям, сверху вниз

Полный чек **с нуля**, со сценариями пользователя. Порядок пунктов повторяет текст задачи.
Если внутри пункта следующие подпункты выполняются сразу — идём дальше без остановки.

Легенда: **[Я]** — делает ассистент · **[ТЫ]** — нужно от тебя · ✅ — уже сделано и переиспользуется.

---

## 0. Подготовка (перед пунктом 1)

| # | Кто | Действие | Критерий готово |
|---|---|---|---|
| 0.1 | ТЫ | Рестарт сессии + вставить `RESTART_PROMPT.md` | сессия поднялась |
| 0.2 | Я | Эмулятор `Small_Phone` → boot_completed | `adb devices` = device |
| 0.3 | Я | APK через MCP `app:install` (поверх, без wipe) | `Success` |
| 0.4 | Я | Web dev-сервер `:8080` (с CORS-proxy) | HTTP 200, `<title>Jarvis</title>` |
| 0.5 | Я | Проверка WebGL в Chrome через `CHROME_PATH` | `canvas` есть → web-драйвер годен |
| 0.6 | Я | **Доклад готовности:** Android ✅/❌, Web ✅/❌ | ждём твои сценарии |

> Развилка: если WebGL `false` — **[ТЫ]** подключаешь расширение `claude.ai/chrome` (логин тем же аккаунтом +
> перезапуск Chrome). Android не зависит — поедет в любом случае.

---

## 1. Уровень 1 — код: unit/integration тесты на бизнес-логику

> «дайте ассистенту промпт: он сам находит непокрытые модули, пишет тесты, запускает, убеждается что проходят»
> «минимум 3 файла, тесты должны пройти с первого запуска»

| # | Кто | Действие | Критерий готово |
|---|---|---|---|
| 1.1 | ТЫ | Выдаёшь промпт L1 (готовый — `prompts.md`, раздел «Уровень 1»; можешь править) | промпт получен |
| 1.2 | Я | Сам ищу непокрытые модули бизнес-логики (мапперы, репозитории, use-cases, датасорсы) | список непокрытого |
| 1.3 | Я | Пишу тесты в `src/commonTest` по конвенциям проекта | ≥ **3 файла** |
| 1.4 | Я | Запускаю `:feature:chat:testAndroidHostTest :feature:ai:testAndroidHostTest` | **зелёные с первого запуска**, 0 failed |
| 1.5 | Я | Отчёт: файлы, число тестов, вывод прогона, найденные продовые дефекты | отчёт приложен |

✅ Задел: 5 файлов / 30 тестов уже зелёные — при повторе прогоняю и, если попросишь, добираю непокрытое.

---

## 2. Уровень 2 — UI: smoke через MCP

> «опишите 3–5 пользовательских сценариев текстом» · «агент сам протыкивает UI» ·
> «Playwright MCP (веб) или Claude in Mobile (мобилка)» · «агент сам фиксирует: прошёл или нет, скриншот на каждом шаге, что сломалось»

| # | Кто | Действие | Критерий готово |
|---|---|---|---|
| 2.1 | ТЫ | Даёшь **3–5 сценариев текстом** (формат — `smoke/SCENARIOS_CATALOG.md`, правь как хочешь) | сценарии получены |
| 2.2 | Я | Показываю разложение сценария на шаги **до** исполнения | ты видишь, что я понял верно |
| 2.3 | Я | Протыкиваю UI сам: открываю экраны, жму кнопки, заполняю поля, проверяю результат | все шаги выполнены |
| 2.4 | Я | Драйвер: **Claude in Mobile** (Android) и/или тот же MCP browser-модуль (web) | вызовы MCP, не заглушки |
| 2.5 | Я | Фиксирую: PASS/FAIL, **скриншот на каждом шаге**, что именно сломалось | отчёт + скрины |

---

## 3. Полный цикл

> «агент пишет код-тесты → прогоняет → формирует отчёт» · «агент берёт smoke → протыкивает через MCP → отчёт со скриншотами» ·
> «если что-то упало — агент предлагает, в каком месте проблема»

| # | Кто | Действие | Критерий готово |
|---|---|---|---|
| 3.1 | Я | Цикл L1: пишу → прогоняю → отчёт | `report` по L1 |
| 3.2 | Я | Цикл L2: беру сценарий → протыкиваю через MCP → отчёт со скринами | `smoke/*/report.md` |
| 3.3 | Я | На любом FAIL — называю шаг, ожидание, факт и **предполагаемое место проблемы** (модуль/функция) | диагноз, а не «упало» |

---

## 4. Усиление

> «встройте оба уровня в flow: после PR агент прогоняет код-тесты и smoke, собирает единый отчёт»
> «добавьте сценарий: "я задеплоил новую фичу — обнови smoke-сценарии и прогони всё заново"»

| # | Кто | Действие | Критерий готово |
|---|---|---|---|
| 4.1 | Я | Профиль `pr-check`: после PR → L1 + L2 → **единый отчёт** → вердикт MERGE-READY/BLOCKED | `pr-check.md` |
| 4.2 | Я | Единый отчёт по факту прогона | `pr-check-report.md` |
| 4.3 | ТЫ | Триггеришь: «я задеплоил новую фичу — обнови smoke и прогони всё заново» | команда дана |
| 4.4 | Я | Обновляю сценарии под изменённый UI и гоняю оба уровня заново | обновлённые сценарии + новый отчёт |

---

## 5. Результат (критерии приёмки)

- [ ] Код-тесты на **3+ модуля**, зелёные с первого запуска
- [ ] **3–5 smoke-сценариев**, которые агент прогоняет через MCP сам
- [ ] Отчёт **со скриншотами** на каждом шаге
- [ ] Интеграция в flow разработки (`pr-check` + сценарий «задеплоил фичу → обнови и прогони»)

---

## Что нужно от тебя — коротко

1. Рестарт + вставить `RESTART_PROMPT.md`.
2. Дождаться моего доклада готовности (Android/Web).
3. Выдать промпт L1 (или сказать «бери готовый из prompts.md»).
4. Выдать 3–5 своих сценариев для L2.
5. В конце — триггернуть «задеплоил фичу, обнови smoke и прогони заново».

Всё остальное — на мне.
