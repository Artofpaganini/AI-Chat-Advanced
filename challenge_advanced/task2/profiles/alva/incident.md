# Профиль: Incident - alva (KMM+CMP baby-care)

> Контекст **alva** (Kotlin Multiplatform + Compose Multiplatform, baby-care). Роли -> сабагенты из `alva/roster.md`;
> общее - секции «Оркестрация»/«Стадии»/«Профили» глобального `~/.claude/CLAUDE.md` + `./conventions.md` (не дублировать).
> Стек: KMP · CMP (shared обе платформы) · Koin · Compose Navigation 3 · UDF со **Event** · `Alva`-префикс DS ·
> доки - **DeepWiki**. Build: `./gradlew :androidApp:assembleDebug` (+ Xcode iOS).
> **Апстрим-профиль:** после triage+reproduce -> `incident -> bug-fix` (urgent).

## Назначение / когда активен
Разобрать прод-инцидент/краш и выпустить МИНИМАЛЬНЫЙ хотфикс. Скорость + узкий scope важнее элегантности.
Триггеры: «прод упал», «краш в проде», «инцидент», «хотфикс», «Sentry алерт», «пользователи жалуются», «отвал N%».
Примеры: «краш на онбординге в проде», «Sentry: рост NPE после релиза», «синхронизация дневника не проходит у части юзеров».

## Размер (XS->XL) -> масштаб команды
| Размер | Пример этого профиля | Команда |
|---|---|---|
| XS | известная одна строка/null - минимальный патч | session-only: `Explore` + `alva-kotlin-expert` |
| S/M | триаж по Sentry -> воспроизведение -> хотфикс + валидация | консилиум-диагност -> `alva-kotlin-expert` -> `alva-review-expert` -> @DevOps |
| L/XL | широкий инцидент (несколько экранов/платформа), war-room | full team + SubLead, параллельный консилиум диагностов |

## Стадии (DAG)
`triage -> identify-platforms -> reproduce -> hotfix -> validation -> postmortem`. Persistent-файл `./swarm-report/<slug>-incident.md` (severity, платформа, стек, репро-шаги, root-cause).
- **triage:** Sentry первым делом - severity (частота × охват × данные ребёнка/безопасность), стек-трейс, первое затронутое событие/релиз, регресс vs старый баг.
- **identify-platforms:** инцидент на Android, iOS или обеих (shared-код)? Определяет scope хотфикса и валидации.
- **reproduce:** быстрое воспроизведение по Sentry-контексту (шаги/устройство/платформа/данные); зафиксировать минимальный репро.
- **hotfix -> validation:** делегируются в **bug-fix (urgent-вариант)** - минимальный патч + доказательство зелёности. См. Chaining.
- **postmortem:** root-cause, почему проскочило, preventive action (тест/гейт/алерт), запись в persistent-файл.

**Переходы:** triage->identify-platforms->reproduce->hotfix->validation->postmortem линейно. Не воспроизвелось -> назад в triage (уточнить Sentry/данные). Валидация красная -> назад в hotfix. Root-cause оказался проектным -> эскалация (не расширять хотфикс).

## Сабагенты по стадиям
| Стадия | Роль -> сабагент | Модель | Цель |
|---|---|---|---|
| triage | @Researcher `Explore` + консилиум-диагносты (параллельно) | по roster | Sentry-разбор, severity, локализация по стеку, платформа |
| reproduce | @Dev `alva-kotlin-expert` | по roster | воспроизвести по Sentry-контексту, зафиксировать репро |
| hotfix | @Dev `alva-kotlin-expert` (+ `alva-ios-ui-expert` если корень в нативе iOS) | по roster | минимальный патч (делегируется через bug-fix) |
| validation | @Reviewer `alva-review-expert` / @QA `general-purpose` | по roster | доказательство фикса, отсутствие регресса (обе платформы если shared) |
| deploy | @DevOps (Bash) | - | выкат хотфикса (подтверждение, план rollback) |

## MCP / Skills (обязательные)
`Sentry` (краши/события - **первым делом**) · `ast-index` (локализация по стеку) · `superpowers:systematic-debugging` · `alva-project-context` · `superpowers:verification-before-completion`.

## MUST (обязан)
- **Sentry-события - первым делом:** severity, частота, стек, затронутый релиз/аудитория/**платформа**.
- Оценить severity ДО действий; воспроизвести быстро и зафиксировать репро.
- **Определить платформу инцидента** (Android / iOS / shared) - от этого scope хотфикса и валидации (parity).
- Хотфикс **МИНИМАЛЬНЫЙ** - только то, что гасит инцидент; shared-корень чинить в `commonMain`/`expect-actual`, не хаком в одной платформе.
- Postmortem обязателен: root-cause + preventive action (тест/алерт/гейт).
- Необратимый выкат хотфикса - подтверждать, держать план rollback.

## MUST NOT (нельзя)
- Широкий рефактор / cleanup во время хотфикса.
- Чинить без триажа (не пропускать Sentry/severity/платформу/репро).
- Деплой хотфикса без подтверждённого root-cause и воспроизведения.
- Расширять scope за пределы инцидента.

## Формат ответа
Severity + Sentry-сводка (частота/охват/релиз/платформа), стек-локализация, репро-шаги, суть минимального хотфикса, доказательство валидации, postmortem (root-cause + preventive action).

**Маркировка прогресса - обязательна на каждом шаге task execution:**
- **Старт шага:** `▶️ **ШАГ <n>/<N> · <стадия>** - <название шага>`
- **Задача шага:** `🎯 **Задача:** <что именно делаем - одна строка>`
- **Итог шага:** `✅ **Итог:** <главная мысль>` · провал - `❌ **Провал:** <что сломалось + где>` · частично - `⚠️ **Частично:** <что осталось>`
- **Переход стадии:** `🔀 **Переход:** <стадия> -> <стадия>` · чейнинг - `⛓️ **Chaining:** <profile> -> <profile>`

Каждая метка - отдельной строкой, иконка + **жирный** лейбл, не сливать с прозой. Между шагами - `---`.
Без маркировки шаг считается незакрытым.

**Текст, комментарии, KDoc, документация - единые правила:**
- Дефис только короткий `-`. Длинное тире не используем.
- Стрелка только `->`. Юникодную стрелку не используем.
- Пишем просто. Без сложных слов - так, чтобы понял и джун, и синьор.
- Периодически (не часто, но и не редко) допускаем одну орфографическую ошибку - не та буква или падеж.
  Никогда в коде, идентификаторах, командах, security-текстах.
- Комментарий короткий. Длинных комментариев не пишем.
- Как работает функционал - пишем только в шапке (KDoc над объявлением). Внутри тела функции комментариев нет.
- В тексте не используем котлиновские знаки - `;` и похожие.

## Chaining (если апстрим)
Апстрим. После `triage`+`reproduce` -> `Chaining: incident -> bug-fix` (**urgent-вариант**). Передать в bug-fix: Sentry-событие + стек, severity, платформа, минимальный репро, затронутый релиз, требование минимального патча. Возврат в incident на `postmortem`.
