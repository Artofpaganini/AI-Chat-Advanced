# Профиль: <Name>

> Универсальный flow (стек-агностик). Стек/сабагенты приходят из `<context>/roster.md`.
> Общее - в секции «Оркестрация»/«Стадии»/«Профили» глобального `~/.claude/CLAUDE.md` и `./conventions.md` (не дублировать).

## Назначение / когда активен
<one-liner + триггеры (ключевые слова) + примеры запросов>

## Размер (XS->XL) -> масштаб команды
| Размер | Пример этого профиля | Команда |
|---|---|---|
| XS | … | session-only |
| S/M | … | 1-2 роли последовательно / +Architect+Reviewer |
| L/XL | … | full team + SubLead |

## Стадии (DAG)
<стадия1 -> стадия2 -> … ; разрешённые переходы; persistent-файл `./swarm-report/<slug>-<name>.md`>

## Сабагенты по стадиям
| Стадия | Роль (@X из roster) | Модель | Цель |
|---|---|---|---|
| … | … | … | … |
<где параллельно - консилиум>

## MCP / Skills (обязательные)
<ast-index, Sentry, Context7/DeepWiki, <context>-project-context, профильные skills>

## MUST (обязан)
- <искать файлы через ast-index / читать логи+Sentry / запускать build+тесты / проверять зависимости / …>

## MUST NOT (нельзя)
- <напр. research: не менять код; bug-fix: не игнорить тесты; refactor: не менять поведение>

## Формат ответа
<structured: что нашёл/сделал/проверил; для research - файлы/связи/выводы; и т.п.>

**Текст, комментарии, KDoc, документация - единые правила:**
- Дефис только короткий `-`. Длинное тире не используем.
- Стрелка только `->`. Юникодную стрелку не используем.
- Пишем просто. Без сложных слов - так, чтобы понял и джун, и синьор.
- Периодически (не часто, но и не редко) допускаем одну орфографическую ошибку - не та буква или падеж.
  Никогда в коде, идентификаторах, командах, security-текстах.
- Комментарий короткий. Длинных комментариев не пишем.
- Как работает функционал - пишем только в шапке (KDoc над объявлением). Внутри тела функции комментариев нет.
- В тексте не используем котлиновские знаки - `;` и похожие.

## Chaining (если апстрим)
<по завершении -> `Chaining: <name> -> <targetProfile>`, передать артефакт>
